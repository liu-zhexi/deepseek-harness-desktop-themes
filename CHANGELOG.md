# Changelog

## 1.4.0 — 2026-08-16

### Added

- 阮启岚快捷动作面板：说话、打篮球、铁山靠、挥手与随机动作均可直接触发并支持键盘操作。
- DSH web 一键部署：构建时生成唯一开发版本，安装后校验 `client.js` / `index.js` 哈希并可自动重启。
- 设置页显示插件版本与实际构建编号，便于确认浏览器是否加载了最新代码。
- 动作资源、屏幕边缘和生成脚本安全回归测试。

### Changed

- 四组动作改为按动作延迟初始化、当前动作空闲预热；运行帧降至 640px 高，客户端包体积降低约 34%。
- 铁山靠、篮球、挥手和说话动作增加峰值停顿与回弹时序；重复触发同一动作会从首帧重新播放。
- 宽动作在屏幕边缘自动水平回推，避免篮球或身体被裁切。
- 单击菜单立即响应，300ms 内第二次点击触发随机动作，不再延迟第一次点击。
- DashScope 脚本只从 `DASHSCOPE_API_KEY` 环境变量读取凭据；生成脚本全部使用项目相对路径。

## 1.3.0 — 2026-08-15

新增"小程序员"桌面小人形象（从参考图提取的真人透明底形象）。

### Added

- **小程序员（photo）形象**：从参考图片中提取戴眼镜的小男孩程序员（AI 定位 + 裁剪 + 背景移除 + 色度键抠图，透明底 PNG），内嵌为 base64 随客户端 bundle 发布，无运行时网络请求。
  - 成为默认形象；脸部/姿态完全保留原图，未重新绘制。
  - 原有幽灵/史莱姆/小猫三款 SVG 形象保留，可在快捷菜单或设置页切换。

## 1.2.0 — 2026-08-15

新增桌面小人（桌面宠物）。

### Added

- **桌面小人**：悬浮在 DSH 窗口之上的可交互角色，注册进 `shell.overlay` 浮动层。
  - 三款形象：幽灵 / 史莱姆 / 小猫，纯 SVG 绘制、随主题强调色着色。
  - 可拖拽移动（位置以视口百分比持久化）、待机动画（漂浮 / 眨眼 / 咖啡热气）、气泡台词 “debug + coffee = life”。
  - 单击小人打开快捷菜单：主题 / 字体 / 壁纸 / 特效 / 小人五页签，直接切换，无需进入设置页。
  - 尊重 `prefers-reduced-motion`、动画开关与省电性能档；隐藏后可经“设置 → 桌面外观 → 桌面小人”重新开启。
  - 配置持久化：`pet` 字段（enabled / style / positionX / positionY / size / animations / speech）接入 schema 与校验迁移。

## 1.1.0 — 2026-02-15

重大升级：设置交互重做、六套主题、光影与粒子系统、自定义配色、持久化壁纸与完整配置持久化。

### Added

- 六套签名主题：量子蓝（科技）、极光幻境（空灵）、薄荷清风（清新）、樱雾（柔和）、日落流光（温暖）、黑金星穹（高级），均带完整 token（背景/面板/侧边栏/悬停/选中/文字/强调/边框/阴影/光晕/成功/警告/错误/代码块/滚动条/粒子/光影）。
- 可视化主题卡片（背景/面板/文字/强调色/粒子缩略 + 名称 + 风格标签）。
- 字体预设选择器：界面 8 组 + 代码 8 组，每个选项用对应字体渲染预览，显示“已安装/回退”，并保留自定义字体输入。
- Canvas-2D 粒子/光影引擎：10 种效果预设（科技数据流、星空、极光流体、萤火、气泡、樱花、金尘、柔光呼吸等），rAF 驱动、可见性暂停、`prefers-reduced-motion` 降级、DPR 钳制、尺寸防抖、复用 Canvas。
- 自定义配色主题：7 色编辑 + 7 种一键协调配色（同色系/邻近/互补/冷/暖/柔和低饱和/高对比）+ 对比度检测与一键修正 + 命名/保存/复制/删除；内置主题可复制后编辑。
- 壁纸完整体验：拖放、最近使用、预览、填充方式、缩放/透明度/模糊/亮度/饱和度/暗色遮罩/主题色混合；IndexedDB 持久化（不再依赖临时 blob URL）。
- 性能等级（省电/均衡/高质量）、动画速度（静止/舒缓/标准/活跃）、光影强度、粒子密度等枚举控件。
- 配置持久化增强：`schemaVersion` 2、v1→v2 迁移、防抖写入、保存状态提示、数据损坏自动回退。

### Changed

- 设置页重构为 9 个分组：主题风格 / 字体排版 / 壁纸 / 光影效果 / 粒子效果 / 透明与毛玻璃 / 自定义配色 / 动画与性能 / 导入导出重置。
- 旧主题 Tokyo Night / Catppuccin Mocha / 黑金 迁移映射为量子蓝 / 极光幻境 / 黑金星穹；旧字体名迁移为预设；旧 `glass.performanceMode` 迁移为 `blurLevel`。

### Removed

- 自由文本字体输入从普通设置移除（保留在“自定义字体”与高级区域）。

## 1.0.0 — 2026-02-15

首个可安装版本。

- 三套内置主题、字体系统、半透明背景、自定义壁纸、毛玻璃、动画与性能、导入导出、重置、设置页、宿主设置命名空间、50 个单元测试。
